#include<iostream>
#include <bits/stdc++.h>
int main()
{
    long q;
    std::cin >> q;
    long ans[q];
    for(long a0 = 0; a0 < q; a0++)
    {
        long n;
        std::cin>>n;
        long arr[n];
        for(long i=0;i<n;i++)
        {
            std::cin>>arr[i];
            std::cout<<(1^2);
        }
    }
    // for(long i=0;i<q;i++)
    // {
    //     std::cout<<ans[i]<<"\n";
    // }
    return 0;
}