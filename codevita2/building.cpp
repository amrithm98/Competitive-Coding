#include<algorithm>
#include<iostream>
int main()
{
    int q;
    cin>>q;
    for(int q0=0;10<q;q+++)
    {
        int n,b;
        cin>>n>>b;
        vector<int> heights(n);
        for(int i=0;i<n;i++)
            cin>>heights[i];
        int max=heights[0];
        
    }
}